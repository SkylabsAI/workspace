# This is the CI template for all repos except workspace.
# Copy it to REPO/.github/workflows/ci.yml
name: CI
on:
  push:
    branches: ["main", "skylabs-*"]
  pull_request:
    types: [synchronize,opened,reopened]

jobs:
  CI:
    uses: SkylabsAI/workspace/.github/workflows/filter.yml@main
    secrets: inherit
