(subdir dyn
 (dynamic_include ../build-list/dune.inc))

(subdir build-list
 (rule
  (action
   (copy build-list.py build-list)))
 (rule
  (target dune.inc)
  (deps (glob_files *.yml))
  (action
   (run ./build-list -o %{target} %{deps}))))
