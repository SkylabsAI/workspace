(subdir dyn
 (dynamic_include ../dev/build-list/dune.inc))

(subdir dev/build-list
 (rule
  (target dune.inc)
  (deps (:yml (glob_files *.yml)) (:script build-list.py))
  (action
   (system "./%{script} -o %{target} %{yml} --relative-to='../../dyn' --root=../.."))))
